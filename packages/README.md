# Packages

`packages/` is reserved for normal app/vendor package space.

LO language, runtime, target, tooling and domain packages now live in
`packages-lo/`.

Do not add LO package contracts here. Use this directory only when the host app
needs ordinary ecosystem packages that are not part of the LO package
collection.
